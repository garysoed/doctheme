<div class="class">
  <section class="class-summary">
    <header>
      <code>{{#if access}}<span class="keyword">{{access}}</span> {{/if}}{{#if static}}<span class="keyword">static</span> {{/if}}{{moduleName}}{{#if extends}} <span class="keyword">extends</span> {{#crossLink extends}}{{/crossLink}}{{/if}}</code>
      {{!-- TODO(gs): Add subclasses --}}
    </header>

    <div class="content description">
      {{{classDescription}}}
    </div>

  </section>
  {{#if example}}
    <section class="collapsible class-subsection">
      <header>
        <h2>Examples</h2>
      </header>
      <div>
        <pre class="code prettyprint">
          {{{example}}}
        </pre>
      </div>
    </section>
  {{/if}}

  {{#unless access}}
  {{#if is_constructor}}
    <section class="collapsible class-subsection">
      <header>
        <h2>Constructor</h2>
      </header>
      <div>
        {{#is_constructor}}
          {{>method}}
        {{/is_constructor}}
      </div>
    </section>
  {{/if}}
  {{/unless}}

  {{#if properties}}
    <section class="collapsible class-subsection">
      <header>
        <h2>Properties</h2>
      </header>
      <div>
        {{#properties}}
          {{>props}}
        {{/properties}}
      </div>
    </section>
  {{/if}}

  {{#if methods}}
    <section class="collapsible class-subsection">
      <header>
        <h2>Methods</h2>
      </header>
      <div>
        {{#methods}}
          {{>method}}
        {{/methods}}
      </div>
    </section>
  {{/if}}

  {{#if events}}
    <section class="collapsible class-subsection">
      <header>
        <h2>Events</h2>
      </header>
      <div>
        {{#events}}
          {{>events}}
        {{/events}}
      </div>
    </section>
  {{/if}}
</div>

{{!-- {{#if uses}}
  Uses:
  {{#each uses}}
      <a href="{{this}}.html">{{this}}</a>
  {{/each}}
  <br>
{{/if}}
{{#if extension_for}}
    Extension For:
    {{#each extension_for}}
        <a href="{{this}}.html">{{this}}</a>
    {{/each}}
    <br>
{{/if}}
{{#if extends}}
    Extends: {{#crossLink extends}}{{/crossLink}}<br>
{{/if}}
<span class="foundat">Class defined in: <a href="../{{foundAt}}">`{{file}}:{{line}}`</a></span>
<div class="intro">{{{classDescription}}}</div> --}}


    {{!-- <div>
        {{#if attrs}}
            <div id="attrs">
            {{#attrs}}
                {{>attrs}}
            {{/attrs}}
            </div>
        {{/if}}
        {{#if events}}
            <div id="events">
            {{#events}}
                {{>events}}
            {{/events}}
            </div>
        {{/if}}
    </div> --}}
