<!DOCTYPE html>
<html lang="en">
<head>
  <base href=".">

  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="theme-color" content="{{projectColorPrimary}}">

  <title>{{htmlTitle}}</title>

  <link rel="stylesheet" href="{{projectAssets}}/vendor/prettify/prettify-min.css">

  <link rel="stylesheet" href="{{projectAssets}}/css/class.css">
  <link rel="stylesheet" href="{{projectAssets}}/css/events.css">
  <link rel="stylesheet" href="{{projectAssets}}/css/index.css">
  <link rel="stylesheet" href="{{projectAssets}}/css/main.css">
  <link rel="stylesheet" href="{{projectAssets}}/css/method.css">
  <link rel="stylesheet" href="{{projectAssets}}/css/prettyprint.css">
  <link rel="stylesheet" href="{{projectAssets}}/css/props.css">
  <link rel="stylesheet" href="{{projectAssets}}/css/sidebar.css">
  <style>
    @import url(http://fonts.googleapis.com/css?family=Ubuntu+Mono:400,700|Roboto:400,700);

    body {
      display: flex;
      flex-direction: column;
      font-family: 'Roboto', Helvetica, Arial, sans-serif;
      font-size: smaller;
      line-height: 3ex;
      margin: 0;
      height: 100vh;
    }

    .description code {
      border-radius: 4px;
      padding: 2px 4px;
      white-space: pre;
    }

    .since {
      font-family: 'Ubuntu Mono', monospace;
      font-size: 0.8rem;
      height: 25px;
      line-height: 25px;
      text-transform: uppercase;
      overflow: hidden;
    }

    .since {
      font-family: 'Ubuntu Mono', monospace;
      font-size: 0.8rem;
      line-height: 2ex;
      text-transform: uppercase;
    }

    h1, h2 {
      margin: 5px 0;
    }

    a, a:visited {
      text-decoration: none;
    }

    pre,
    code,
    a.crosslink {
      font-family: 'Ubuntu Mono', monospace;
    }

    .prettyprint code {
      background-color: transparent;
      border-radius: 0;
      padding: 0;
    }

    div.content {
      max-width: 50em;
    }

    section.collapsible > header {
      padding: 1em 2em;
      cursor: pointer;
      -webkit-user-select: none;
      transition: padding .2s;
      position: relative;
    }

    section.collapsible > header h2 {
      font-size: 1.8em;
    }

    section.collapsible > div {
      overflow: hidden;
      transition: height .2s;
    }

    footer {
      padding: 15px;
      -webkit-margin-before: 20px;
    }

    footer.warn {
      border-color: crimson;
      color: crimson;
    }

    dl {
      margin-left: 2em;
      margin-right: 2em;
      padding: 1.5em 2em;
    }

    table, dl {
      border: 1px solid;
    }

    header {
      font-weight: 700;
    }

    p {
      margin-top: 2em;
      margin-bottom: 2em;
    }

    dd p {
      margin-top: 1em;
      margin-bottom: 1em;
    }
  </style>

  <!-- COLORS!!! -->
  <style>
    .button, .main-main a {
      color: {{projectColorPrimary}};
    }
    .button:hover, .main-main a:hover {
      color: {{projectColorAccent}};
    }
    .reverse .button {
      color: {{projectColorLightPrimary}};
    }
    .reverse .button:hover {
      color: {{projectColorAccent}};
    }
    .reverse .button.primary {
      color: {{projectColorBackground}};
    }
    .reverse .button.primary:hover {
      color: {{projectColorAccent}};
    }

    .main-menu-button:hover {
      color: {{projectColorAccent}};
    }

    body {
      background-color: {{projectColorBackground}};
      color: {{projectColorForeground}};
    }
    .reverse {
      background-color: {{projectColorPrimary}};
      color: {{projectColorLightPrimary}};
    }

    table, dl {
      border-color: {{projectColorPrimary}};
    }

    .since {
      color: {{projectColorDarkPrimary}};
    }
    .reverse .since {
      color: {{projectColorLightPrimary}};
    }

    header.main-header {
      -webkit-filter: drop-shadow(0px 2px 5px {{projectColorDarkPrimary}});
    }
    header.main-header.scroll-top {
      -webkit-filter: none;
    }

    li.sidebar-selected {
      border-right: 5px solid {{projectColorAccent}};
    }

    .reverse section.collapsible.collapsed > header {
      color: {{projectColorLightPrimary}};
    }

    .description code {
      color: {{projectColorForeground}};
      background-color: {{projectColorLightPrimary}};
    }

    code .keyword {
      color: {{projectColorForeground}};
    }

    .description pre code {
      background-color: transparent;
    }

    pre.prettyprint {
      border-color: {{projectColorPrimary}};
    }

    header.tutorial {
      color: {{projectColorDarkPrimary}};
    }

    footer {
      border: 2px solid {{projectColorPrimary}};
    }

    .divider {
      border-color: {{projectColorBorder}};
    }
  </style>
</head>
<body>
  <header class="main-header reverse scroll-top">
    <div class="main-menu-button button primary">&#9776;</div>
    <div class="main-header-section">
      <h1><a class="button primary" href="{{projectRoot}}index.html">{{projectName}}</a></h1>
      <div class="since">v {{projectVersion}}</div>
    </div>
    <a class="button" href="{{projectUrl}}">GitHub</a>
  </header>
  <section class="main-content">
    <section class="main-sidebar reverse">{{> sidebar}}</section>
    <section class="main-main">{{> layout_content}}</section>
  </section>
</body>
  <script src="{{projectAssets}}/vendor/prettify/prettify-min.js"></script>
  <script>prettyPrint();</script>
  <script src="{{projectAssets}}/js/selection.js"></script>
  <script src="{{projectAssets}}/js/collapse.js"></script>
  <script src="{{projectAssets}}/js/scroll.js"></script>
  <script src="{{projectAssets}}/js/xhr.js"></script>
</html>
